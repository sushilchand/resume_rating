[project]
name = "resume-rating"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sushil Chand", email = "sushilchand@Sushils-MacBook-Air.local" }
]
requires-python = ">=3.12"
dependencies = [
    "groq>=1.7.0",
    "pydantic>=2.13.5",
    "pypdf>=6.19.0",
    "python-dotenv>=1.2.3",
]

[project.scripts]
resume-rating = "resume_rating:main"

[build-system]
requires = ["uv_build>=0.12.14,<0.13.0"]
build-backend = "uv_build"
